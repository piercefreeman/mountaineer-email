# example_app

Showcase defining and integrating an email template with a Mountaineer app. Sending emails are not tested because you'll need.

## Getting Started

Start the local Docker stack:

```bash
docker compose up --build
```
