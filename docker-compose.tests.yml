version: "3.8"

services:
  postgres:
    command: -c 'max_connections=200'
    image: postgres:16
    environment:
      POSTGRES_USER: mountaineer_daemons
      POSTGRES_PASSWORD: mysecretpassword
      POSTGRES_DB: mountaineer_daemons_test_db
    ports:
      - "5434:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
